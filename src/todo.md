Rewrite Board to use two loops to make the squares instead of hardcoding them. PR-0001
Display the location for each move in the format (col, row) in the move history list. PR-0002
Bold the currently selected item in the move list.  PR-0003
Add a toggle button that lets you sort the moves in either ascending or descending order. PR-0004
When someone wins, highlight the three squares that caused the win. PR-0005
When no one wins, display a message about the result being a draw. PR-0006